<p>
  <strong>Title:</strong>
  <%= @product.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

  <h2>Leave your feedback for this product</h2>
  <%= form_for([@product, @product.reviews.build]) do |f| %>
    <p>
      <%= f.label :msg %><br>
      <%= f.text_area :msg %>
    </p>
    <p>
      <%= f.submit %>
    </p>
  <% end %>


<h2>Reviews</h2>
<% @product.reviews.each do |review| %>

  <p>
    <strong>Review:</ctrong>
    <%= review.msg %>
  </p>

  <p>
    <strong>HERE ADD USER NAME:</ctrong>
  </p>

  <p>
    <%= link_to 'Delete review', [review.product, review],
                 method:  :delete,
                 data: { confirm: 'Are you sure?' } %>
  </p>
<% end %>



<br/>
<%= link_to 'Back', products_path %>
<%= link_to 'Edit', edit_product_path(@product) %>
     
